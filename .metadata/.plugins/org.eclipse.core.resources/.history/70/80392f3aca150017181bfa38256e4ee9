//入门程序
Hello ${abc} !


//javaBean
${person.id?c}
${person.id}
${person.name}

//list
<#list list as l>
	${l_index+101}
	${l.id}
	${l.name}
</#list>


//map方式一
<#list map?keys as k>
	${map[k].id}
	${map[k].name}
</#list>

//方式二
${map.m1.id}
${map.m1.name}
${map.m2.id}
${map.m2.name}


//List<Map>
<#list lm as l >
	<#list>
	
	</#list>
</#list>







